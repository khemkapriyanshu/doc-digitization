# doc-digitization
end-to-end document automation pipelines using OCR + LLMs for structured data extraction, intelligent form parsing, and database recording. • Integrated Vision Language Model to reduce manual processing and enable scalable, layout-independent document digitization and deployed databases and language models locally enabling offline remote operations
